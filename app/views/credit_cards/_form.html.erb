<%= simple_form_for(@credit_card) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :card_num %>
    <%= f.input :expiry_date %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
